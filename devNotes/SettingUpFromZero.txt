In order to run this project, you'll need to have a MongoDB account and create a cluster.

After you have set up a custer, add the connection string to the .env file, then run "create_counters.js" to create counters used for Idea and Project documents' IDs.
